<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Thiskord_Front.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thiskord_Front"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">


    <StackPanel Orientation="Vertical" Spacing="20"
     HorizontalAlignment="Center" 
     VerticalAlignment="Center">
        <TextBlock Text="Connexion"
     FontFamily="Arial"
     FontSize="24" 
     FontStyle="Italic" 
     TextWrapping="WrapWholeWords"
     CharacterSpacing="200" 
     Foreground="CornflowerBlue"
    HorizontalAlignment="Center"/>

        <TextBox Width="250" Margin="0,0,8,0" Header="Nom" PlaceholderText="Username"
    FontFamily="Arial"
    HorizontalAlignment="Center"
    VerticalAlignment="Center" />

        <PasswordBox Header="Mot de passe" Name="passworBoxWithRevealmode" PlaceholderText="Password"
    Width="250" Margin="0,0,8,0" Height="70"
    PasswordRevealMode="Hidden" 
    AutomationProperties.Name="Sample password box"/>

        <Button Style="{StaticResource AccentButtonStyle}"
    Content="Connexion"
    FontFamily="Arial"
    FontSize="24"
    HorizontalAlignment="Center"
    VerticalAlignment="Center"
     />
        <Button Style="{StaticResource SubtleButtonStyle}"
            Content="Inscription"
       FontFamily="Arial"
       FontSize="24"
       HorizontalAlignment="Center"
       VerticalAlignment="Center"  />
    </StackPanel>
</Page>