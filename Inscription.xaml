<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Thiskord_Front.Inscription"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Thiskord_Front"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Style="{StaticResource LoginBackgroundStyle}" Orientation="Vertical" Spacing="20"
 HorizontalAlignment="Center" 
 VerticalAlignment="Center">

        <TextBlock Text="Inscription"
         FontFamily="Arial"
         FontSize="24" 
         FontStyle="Italic" 
         TextWrapping="WrapWholeWords"
         CharacterSpacing="200" 
        HorizontalAlignment="Center"
    />
        <TextBox Width="250" Margin="0,0,8,0" Height="60" Header="Mail" PlaceholderText="mail" 
          FontFamily="Arial"
         HorizontalAlignment="Center"
         VerticalAlignment="Center" 
     />
        <TextBox Width="250" Margin="0,0,8,0" Height="60" Header="Nom" PlaceholderText="Username" 
        FontFamily="Arial"
        HorizontalAlignment="Center"
        VerticalAlignment="Center" 
    />

        <PasswordBox Header="Mot de passe" Name="passworBoxWithRevealmode" PlaceholderText="Password"
        Width="250" Margin="0,0,8,0" Height="60"
        PasswordRevealMode="Hidden" 
        AutomationProperties.Name="Sample password box"
    />
        <PasswordBox Header="Verification mot de passe" Name="passworBox" PlaceholderText="Password"
        Width="250" Margin="0,0,8,0" Height="60"
        PasswordRevealMode="Hidden" 
        AutomationProperties.Name="Sample password box"
    />

        <Button Style="{StaticResource AccentButtonStyle}"
       Content="Inscription"
       FontFamily="Arial"
       FontSize="18"
       HorizontalAlignment="Center"
       VerticalAlignment="Center"
       Width="250" Margin="0,0,8,0"
    />
    </StackPanel>
</Page>
